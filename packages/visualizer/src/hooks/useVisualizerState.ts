export { useVisualizerState } from "../VisualizerContext";

/**
 * useVisualizerState - Access and update all visualizer state.
 *
 * Example:
 * const { simulationStatus, setSimulationStatus } = useVisualizerState();
 * simulationStatus // 'paused'
 * setSimulationStatus('running')
 */
