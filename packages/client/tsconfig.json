{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "outDir": "dist", // Or adjust based on your build tool
    "rootDir": "src"
  },
  "include": ["src"],
  "references": [{ "path": "../shared" }] // Reference the shared package
  // Assuming a base config might exist at the root
  // "references": [{ "path": "./tsconfig.node.json" }] // If using separate node config
  // "extends": "../../tsconfig.base.json" // If using a base config
}
